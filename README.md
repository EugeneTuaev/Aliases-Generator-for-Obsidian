# Aliases-Generator-for-Obsidian
Проверенный скрипт для автоматической генерации алиасов для заметок в obsidian (ТОЛЬКО на руссом языке) с обращением к 2 API и вшитым офлайн скриптом (только для 1 слова, словосочетания не делает) если нет доступа к инету, с точностью 40-90%

💠 Обращается к 2 API (Морфер ru и Sklonenie ru), комбинирует ответы для максимальной полноты, отбрасывает дубликаты

💠 Если одно апи не отвечает - берет ответ от второго. Если не отвечают оба, то генерирует на основе заложенного офлайн скрипта с точностью около 40-90% (только для одного слова, словосочетания не делает) и пишет комментарий что могут быть ошибки

💠 Отрабатывает в течении 1-5 секунд на среднем железе

💠 Код вынесен в отдельный js файл, так что можно его вызывать одной командой для плагина templater куда требуется (можно не во frontmatter свойства)
```js
<%* tR += await tp.user.aliases(tp) %>
```

💠 Ошибок еще не возникало, но по идее отрабатываются и если название во множественном числе и апи возвращают не все формы, то автоматически идет новый запрос в единственном числе
